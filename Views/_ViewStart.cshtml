@{
    Layout = "_Layout"; 
}

@*This file is used to execude common code before rendering a razor ivew
it usually sets the layout for the views.

all together, the _Layout.cshtml is the template file for any new razor page.
_ViewImports.cshtml is just so you dont have to write imports for every razor file over and over again.
This file, _ViewStart.cshtml is executed before any view, and therefore it is used to define the default layout = _Layout*@